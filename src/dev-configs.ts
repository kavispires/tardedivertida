/**
 * Should be on during development to use firestore (data) emulators
 */
export const USE_FIRESTORE_EMULATOR = true;

/**
 * Should be on during development to use functions emulator
 */
export const USE_FUNCTIONS_EMULATOR = true;

/**
 * Trigger all use mocks (only in development mode)
 */
export const USE_MOCKS = true;

/**
 * Should be on during development to enable development mode (normally displays mock buttons and other dev features)
 */
export const USE_DEV = true;
