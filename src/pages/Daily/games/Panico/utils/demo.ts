/**
 * Button id composition: <number>::<type>::<pool>
 * - number: The button's number in the sequence (starting from 0).
 * - type: The type key of button challenge (constant)
 * - pool: The pool from which the button is drawn (optional, only if there are multiple pools for a type)
 **/

